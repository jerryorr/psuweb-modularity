# latest jquery
npm install --save jquery

# any version >= 1.11.3 but < 2.0.0
npm install --save jquery@1.11.3

# exactly version 1.11.2
npm install --save --save-exact jquery@1.11.2

# updates any modules in package.json that have 
# newer versions matching our rules
npm install

# install all the things
npm install --save jquery lodash backbone bootstrap image-vogue

# install from arbitrary URL
npm install --save image-vogue@https://github.com/SpiderStrategies/vogue/tarball/master
